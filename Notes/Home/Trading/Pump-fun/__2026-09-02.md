
Ναι. Θα το έστηνα ως **execution-aware forecasting**, όχι απλό crossing prediction.
1. Σε κάθε curve update στο slot `N`, το μοντέλο αποφασίζει αν αξίζει `SEND`.
2. Η αγορά θεωρείται ότι εκτελείται πραγματικά στο `N+1`, στην πραγματική curve κατάσταση εκείνου του slot.
3. Το target δεν είναι απλώς «θα γίνει +1.3 crossing», αλλά:
    ​
    `θα γίνει crossing σε H ms ΚΑΙ μετά θα υπάρχει αρκετό momentum ώστε να μπορώ να αγοράσω και να πουλήσω πραγματικά με θετικό net PnL;`
4. Αρχικά όμως θα το σπάσουμε:
    - Model A: crossing μέσα σε π.χ. `500ms`.
    - μετά Model B: post-crossing continuation / διαθέσιμο κέρδος.
5. Το slippage δεν χρειάζεται να είναι αυθαίρετα `10%`. Για κάθε trade υπολογίζουμε **πόσο slippage αντέχει οικονομικά** πριν μηδενιστεί το αναμενόμενο κέρδος.
6. BUY και SELL πρέπει να γίνονται πάνω σε πραγματικές επόμενες slot curves. Αν δεν μπορεί πράγματι να γεμίσει η συναλλαγή εντός του επιτρεπόμενου slippage, θεωρείται failed execution.
7. Το PnL πρέπει να είναι καθαρό:
    ​
    `SELL proceeds - BUY cost - Pump.fun fees - chain fees - priority/Jito fees - slippage`.
8. Τελικός στόχος του optimization δεν είναι WR ή AUC αλλά **πραγματικό SOL/day**, με πρώτο benchmark το `>= 1 SOL/day`.
Το βασικό είναι ότι το crossing γίνεται απλώς το πρώτο forecast target. Η τελική απόφαση πρέπει να βασίζεται στο **αν υπάρχει πραγματικά εκτελέσιμο κέρδος μετά το N+1 BUY**.

Τα EA02 επιβεβαιώνουν ότι το αποτέλεσμα του EA01 **δεν ήταν artifact του oracle SELL**.
- `crossing → πρώτο update → SELL N+1`: **0,982 SOL/day** αν συμπεριλάβουμε και τα αρνητικά evaluable trades.
- `+100ms`: **1,009 SOL/day**.
- `+250ms`: **1,061 SOL/day**.
- `+500ms`: **1,088 SOL/day**.
Άρα ακόμα και χωρίς hindsight επιλογή του καλύτερου SELL, το οικονομικό περιθώριο παραμένει περίπου στον στόχο μας `0,9–1 SOL/day`.
Το `+500ms` φαίνεται καλύτερο οικονομικά:
- 975 monetizable crossings.
- positive-only oracle: **1,282 SOL/day**.
- ακόμα και με floor `>0.01 SOL/trade`: **527 trades → 1,076 SOL/day**.
Και το SELL slippage πλέον φαίνεται λογικό: στο `+500ms` p50 `0%`, p75 `0,76%`, p90 `5,284%`. Άρα το προηγούμενο 0% παντού ήταν κυρίως αποτέλεσμα του oracle timing.
Το βασικό συμπέρασμα: **υπάρχει πραγματικό execution-aware περιθώριο. Τώρα έχει νόημα να πάμε στο Model A: σε κάθε fresh curve update, πρόβλεψη αν θα γίνει crossing μέσα στα επόμενα 500ms.**


### roadmap
1. **EA01–EA02: Feasibility audit — DONE**
    - Επιβεβαίωση ότι υπάρχει πραγματικό execution-aware περιθώριο.
    - Στόχος `~0.9–1 SOL/day` φαίνεται εφικτός oracle-wise.
2. **FA01: Crossing predictor**
    - Σε κάθε fresh curve update:
    - `P(+1.3 crossing μέσα στα επόμενα 500ms)`
    - Features: freshness, density, update rate, inter-arrival, burstiness, velocity, acceleration.
3. **FA02: Execution-aware validation**
    - Απόφαση στο `N`.
    - BUY στο πραγματικό `N+1`.
    - Έλεγχος πραγματικού required slippage και net PnL.
4. **FA03: Threshold optimization**
    - Επιλογή probability threshold.
    - Στόχος όχι AUC αλλά `SOL/day`, PnL/trade, PF και αριθμός trades.
5. **FB01: Continuation model**
    - Για predicted crossings:
    - πρόβλεψη αν μετά το crossing υπάρχει αρκετό momentum για κερδοφόρο exit.
6. **FB02: Exit policy**
    - Σύγκριση crossing / `+100 / +250 / +500ms` ή δυναμικού exit.
    - Πραγματικό SELL στο `N+1`.
7. **FC01: Combined strategy**
    - `Crossing probability + continuation probability + execution economics`
    - Τελική απόφαση `SEND / WAIT / SKIP`.
8. **FC02: OOS / shadow validation**
    - Untouched data και μετά shadow runtime.
    - Τελικός στόχος περίπου **≥0.9 SOL/day net** με πραγματικά executable trades.

